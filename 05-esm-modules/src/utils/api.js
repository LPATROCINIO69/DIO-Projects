export const key = {
    value: "345535jsd",
    permission: "admin"
}

export const getDataFromApi = () => {
    console.log(`Apresentando dados da api - value: ${key.value} e permission: ${key.permission}`);
}